#include <stdio.h>

/**
 * main - Entry function
 *
 * Description: Print digit of base 10
 *
 * Return: 0, if success
 */
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
		printf("%d", digit);
	}
	printf("\n");
	return (0);
}
