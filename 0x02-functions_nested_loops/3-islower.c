#include "main.h"

/**
 * _islower - It check lowercase
 * @c: the character to check
 *
 * Decription: A function that check if this is lowercase
 *
 * Return: 1 if c is lower, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
